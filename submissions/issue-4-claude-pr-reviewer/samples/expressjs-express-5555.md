<!-- Unedited output of claude-review. -->
<!-- PR reviewed: https://github.com/expressjs/express/pull/5555 -->
<!-- Command:     python claude_review.py --pr https://github.com/expressjs/express/pull/5555 -->
<!-- Model:       claude-opus-4-8   |   Run: 2026-07-01   |   Tokens: input 1063, output 835 -->

## 🤖 Claude review of #5555 — fix #5554 passing URL instances with new location encoding

## Summary
This PR fixes `res.location` to accept non-string inputs (e.g. `URL` instances) by explicitly coercing the argument with `String(url)` before further processing. It adds a changelog entry and a guarded test verifying a `URL` instance is stringified to its href.

## Identified risks
- `lib/response.js` (line 908): `String(url)` now runs unconditionally. For `null`/`undefined`/`NaN` inputs this produces `"null"`/`"undefined"`/`"NaN"` instead of whatever the prior code path yielded, which is a subtle behavior change for previously-passed falsy values. Worth confirming this matches the intended contract.
- The `"back"`/referrer branch still compares against the original `url` (`if (url === 'back')`). This is correct for strings, but if a caller passed an object whose `String()` equals `'back'`, it would no longer be treated as the alias. Low likelihood but a behavioral inconsistency between `url` and `loc`.

## Improvement suggestions
- Add a test for the `null`/`undefined` case to lock in the coercion behavior and prevent future regressions.
- Consider whether the referrer check should operate on `loc` rather than `url` for consistency now that coercion happens up front.
- The changelog entry lacks a version/date placeholder format matching prior entries; ensure "unreleased changes" is the project's expected convention.

## Confidence score
**High** — The change is a minimal, well-targeted coercion with a covering test; the only concerns are minor edge-case behavior differences for falsy inputs.

---
*Generated by [claude-review] using `claude-opus-4-8`. Advisory only — verify before merging.*
